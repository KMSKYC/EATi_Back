# EATi Backend

> **AI가 추천하는 점심 식사** - 메뉴 추천 및 같이 먹기 매칭 서비스

[![Kotlin](https://img.shields.io/badge/Kotlin-1.9.25-7F52FF?logo=kotlin)](https://kotlinlang.org/)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.5.7-6DB33F?logo=spring)](https://spring.io/projects/spring-boot)
[![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Latest-4169E1?logo=postgresql)](https://www.postgresql.org/)
[![License](https://img.shields.io/badge/License-MIT-blue.svg)](LICENSE)

---

## 📋 목차

- [프로젝트 소개](#-프로젝트-소개)
- [기술 스택](#-기술-스택)
- [아키텍처](#-아키텍처)
- [도메인 설계](#-도메인-설계)
- [프로젝트 구조](#-프로젝트-구조)
- [실행 방법](#-실행-방법)
- [개발 현황](#-개발-현황)

---

## 🎯 프로젝트 소개

**EATi**는 AI 기반 점심 메뉴 추천 및 **함께 먹기 매칭** 서비스입니다.

### 주요 기능 (예정)

- 🍽️ **AI 메뉴 추천**: 사용자 취향 기반 맞춤형 메뉴 추천
- 🗺️ **지도 기반 검색**: 주변 식당 및 메뉴 실시간 검색
- 🏷️ **카테고리 필터**: 한식, 중식, 일식 등 카테고리별 메뉴 탐색
- 👥 **같이 먹기**: 혼밥러를 위한 실시간 매칭 서비스
- 👤 **마이페이지**: 사용자 취향 관리 및 이력 조회

---

## 🛠 기술 스택

### Backend

| 분류 | 기술 스택 |
|------|-----------|
| **Language** | Kotlin 1.9.25 |
| **Framework** | Spring Boot 3.5.7 |
| **Architecture** | Clean Architecture + DDD |
| **ORM** | Spring Data JPA, Spring Data R2DBC (Reactive) |
| **Database** | PostgreSQL (Supabase) |
| **Validation** | Bean Validation (Jakarta) |
| **Monitoring** | Spring Actuator + Prometheus |

### Infrastructure

- **Database**: Supabase (PostgreSQL)
- **Connection Pool**: HikariCP (max: 10, min-idle: 5)
- **CI/CD**: GitHub Actions (OCI 서버 자동 배포)

---

## 🏗 아키텍처

### Clean Architecture + DDD 기반 설계

```
┌─────────────────────────────────────────────────────────────┐
│                     Presentation Layer                       │
│                    (adapter/in/web)                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐      │
│  │    REST      │  │   Request    │  │   Response   │      │
│  │ Controllers  │  │     DTO      │  │     DTO      │      │
│  └──────────────┘  └──────────────┘  └──────────────┘      │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                  Application Layer                           │
│                 (application/*/service)                      │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           Use Case Services (Business Logic)         │   │
│  │  • Service Implementation                            │   │
│  │  • Transaction Management (@Transactional)           │   │
│  │  • DTO Transformation                                │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    Domain Layer                              │
│                   (domain/*/model)                           │
│  ┌──────────────────────────────────────────────────────┐   │
│  │          Domain Models (Business Rules)              │   │
│  │  • Entities (JPA Entity)                             │   │
│  │  • Value Objects                                     │   │
│  │  • Domain Services                                   │   │
│  └──────────────────────────────────────────────────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │
┌────────────────────────▼────────────────────────────────────┐
│                 Infrastructure Layer                         │
│              (adapter/out/persistence)                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │          JPA Repositories (Data Access)              │   │
│  │  • Spring Data JPA (CRUD + Custom Query)             │   │
│  │  • PostgreSQL Database                               │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 의존성 방향

```
Controller → Service → Domain ← Repository
    ↓          ↓         ↓           ↓
  Request    DTO      Entity      Database
```

- **핵심 원칙**: 외부 → 내부 (Domain이 가장 안쪽, 외부 의존성 없음)
- **실용적 설계**: 과도한 추상화 제거, 필요한 곳에만 인터페이스 사용

---

## 📐 도메인 설계

### Bounded Context

```
┌─────────────────────────────────────────────────────────┐
│                    User Context                          │
│  ┌──────────┐              ┌──────────────┐            │
│  │  User    │ ───1:1───▶   │  UserTastes  │            │
│  │ (사용자)  │              │  (선호도)     │            │
│  └──────────┘              └──────────────┘            │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                Restaurant Context                        │
│  ┌─────────────┐          ┌──────────┐                 │
│  │ Restaurant  │ ─N:1───▶ │ Category │                 │
│  │  (식당)      │          │(카테고리) │                 │
│  └─────────────┘          └──────────┘                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   Menu Context                           │
│  ┌──────┐         ┌──────────────────┐                 │
│  │ Menu │ ─1:N──▶ │ RestaurantMenu   │ ◀──N:1─ Restaurant
│  │(메뉴) │         │ (식당별 메뉴)     │                 │
│  └──────┘         └──────────────────┘                 │
└─────────────────────────────────────────────────────────┘
```

### Aggregate 설계

#### 1. User Aggregate

```kotlin
User (Aggregate Root)
├─ userId: String (PK)
├─ email: String
├─ password: String
├─ status: String
├─ nickname: String
├─ birthdate: Date?
├─ gender: String
├─ region: String?
└─ UserTastes (1:1)
   ├─ likedCategoryIds: List<String> (jsonb)
   ├─ dislikedCategoryIds: List<String> (jsonb)
   ├─ recentMenus: List<String>? (jsonb)
   └─ restrictions: List<String>? (jsonb)
```

#### 2. Restaurant Aggregate

```kotlin
Restaurant (Aggregate Root)
├─ restaurantId: String (PK)
├─ categoryId: String (FK → Category)
├─ name: String
├─ address: String
└─ description: String?

Category
├─ categoryId: String (PK)
└─ name: String
```

#### 3. Menu Aggregate

```kotlin
Menu (Aggregate Root)
├─ menuId: String (PK)
├─ categoryId: String (FK → Category)
├─ name: String?
├─ description: String?
└─ imageUrl: String?

RestaurantMenu
├─ restaurantMenuId: String (PK)
├─ menuId: String (FK → Menu)
├─ restaurantId: String (FK → Restaurant)
├─ categoryId: String (FK → Category)
├─ name: String
├─ price: BigDecimal
└─ description: String?
```

---

## 📂 프로젝트 구조

```
src/main/kotlin/msyc/eati/
│
├── domain/                          # 도메인 계층 (비즈니스 로직)
│   ├── user/model/
│   │   ├── User.kt                  # 사용자 Entity
│   │   └── UserTastes.kt            # 사용자 선호도 Entity
│   ├── restaurant/model/
│   │   ├── Restaurant.kt            # 식당 Entity
│   │   └── Category.kt              # 카테고리 Entity
│   └── menu/model/
│       ├── Menu.kt                  # 메뉴 Entity
│       └── RestaurantMenu.kt        # 식당별 메뉴 Entity
│
├── application/                     # 애플리케이션 계층 (유스케이스)
│   ├── auth/service/                # 인증 서비스 (예정)
│   ├── user/service/                # 사용자 서비스 (예정)
│   ├── restaurant/service/          # 식당 서비스 (예정)
│   └── menu/service/                # 메뉴 서비스 (예정)
│
├── adapter/                         # 어댑터 계층
│   ├── in/web/                      # Inbound Adapter (REST)
│   │   ├── IndexController.kt       # 헬스체크 & 인덱스
│   │   ├── auth/
│   │   │   └── AuthController.kt    # 인증 컨트롤러 (예정)
│   │   ├── menu/
│   │   │   └── MenuController.kt    # 메뉴 컨트롤러 (예정)
│   │   └── user/
│   │       └── MyPageController.kt  # 마이페이지 (예정)
│   │
│   └── out/persistence/             # Outbound Adapter (DB)
│       ├── user/
│       │   ├── UserRepository.kt
│       │   └── UserTastesRepository.kt
│       ├── restaurant/
│       │   ├── RestaurantRepository.kt
│       │   └── CategoryRepository.kt
│       └── menu/
│           ├── MenuRepository.kt
│           └── RestaurantMenuRepository.kt
│
├── common/                          # 공통 계층
│   └── util/
│       └── IdGenerator.kt           # NanoId 기반 ID 생성
│
└── config/
    └── DatabaseConfig.kt            # 데이터베이스 설정
```

### 레이어별 역할

| Layer | 책임 | 의존성 |
|-------|------|--------|
| **Domain** | 비즈니스 로직, Entity | 없음 (순수 Kotlin/JPA) |
| **Application** | Use Case 구현, Transaction | Domain |
| **Adapter In** | REST API, DTO 변환 | Application |
| **Adapter Out** | DB 접근 | Domain |
| **Common** | 공통 유틸리티 | 모든 계층에서 사용 |

---

## 🔌 API 엔드포인트

### 헬스 체크

```http
GET /health
```

**Response:**
```json
{
  "status": "OK"
}
```

### 인덱스 페이지

```http
GET /
```

**Response:** HTML 페이지

---

### 구현 예정 API

#### Auth API
- `POST /auth/signup` - 회원가입
- `POST /auth/login` - 로그인

#### Restaurant API
- `GET /restaurant/{id}` - 식당 상세
- `GET /restaurant/nearby` - 주변 식당 검색

#### Menu API
- `GET /menu` - 메뉴 검색
- `GET /menu/map` - 지도 기반 검색
- `GET /menu/category` - 카테고리별 검색

#### User API
- `GET /mypage` - 마이페이지
- `PUT /mypage/profile` - 프로필 수정
- `PUT /mypage/tastes` - 선호도 수정

---

## 🚀 실행 방법

### 사전 요구사항

- **Java 21** 이상
- **PostgreSQL** (또는 Supabase 계정)
- **Gradle 8.14+**

### 환경 변수 설정

`.env` 파일 또는 시스템 환경 변수에 다음을 설정:

```bash
# Database (Supabase PostgreSQL)
JDBC_URL=jdbc:postgresql://[HOST]:[PORT]/[DATABASE]
DB_USERNAME=postgres
DB_PASSWORD=your_password

# R2DBC (Reactive)
R2DBC_URL=r2dbc:postgresql://[HOST]:[PORT]/[DATABASE]
```

### 빌드 및 실행

```bash
# 1. 프로젝트 클론
git clone https://github.com/your-username/EATi_Back.git
cd EATi_Back

# 2. 빌드
./gradlew clean build -x test

# 3. 실행
./gradlew bootRun

# 또는 JAR 직접 실행
java -jar build/libs/EATi-0.0.1-SNAPSHOT.jar
```

### 헬스 체크

```bash
curl http://localhost:8080/health

# Response: {"status":"OK"}
```

---

## 📊 개발 현황

### ✅ 완료된 작업

- [x] 프로젝트 초기 설정
- [x] Clean Architecture + DDD 구조 설계
- [x] 도메인 모델 설계 (6개 Entity)
- [x] Repository 계층 구현
- [x] 데이터베이스 연결 (Supabase PostgreSQL)
- [x] CI/CD 파이프라인 구축 (GitHub Actions)

### 🔄 진행 중

- [ ] 인증/인가 (JWT)
- [ ] 사용자 관리 기능
- [ ] 메뉴 추천 기능
- [ ] 식당 검색 기능

### 📋 계획 중

- [ ] AI 메뉴 추천 알고리즘
- [ ] 지도 기반 검색
- [ ] 같이 먹기 매칭 서비스
- [ ] API 문서화 (Swagger)
- [ ] 테스트 코드 작성

---

## 🗄 데이터베이스 스키마

### ERD 요약

```
users (1) ────── (1) users_tastes
  │
  │ (Author)
  ↓
[Together Posts - 예정]

restaurants (N) ────── (1) category
  │
  │ (N:1)
  ↓
restaurant_menus (N) ────── (1) menus (N) ────── (1) category
```

### 주요 테이블

| 테이블 | 설명 | 주요 컬럼 |
|--------|------|-----------|
| `users` | 사용자 | user_id, email, password, nickname |
| `users_tastes` | 사용자 선호도 | user_id, liked_category_ids (jsonb) |
| `restaurants` | 식당 | restaurant_id, name, address |
| `category` | 카테고리 | category_id, name |
| `menus` | 메뉴 | menu_id, name, image_url |
| `restaurant_menus` | 식당별 메뉴 | restaurant_menu_id, price |

---

## 📈 성능 최적화

- **HikariCP**: Connection Pool (max: 10, min-idle: 5)
- **@Transactional(readOnly = true)**: 읽기 전용 트랜잭션 최적화 (예정)
- **Pagination**: 무한 스크롤 지원 (예정)
- **Index**: DB 인덱스 설정 (예정)

---

## 🔐 보안

- [ ] **JWT 인증** (예정)
- [ ] **비밀번호 암호화** (예정)
- [ ] **CORS 설정** (예정)
- [ ] **Input Validation** (예정)
- [x] **SQL Injection 방지** (JPA 사용)

---

## 🧪 테스트

```bash
# 단위 테스트
./gradlew test

# 통합 테스트
./gradlew integrationTest
```

---

## 💡 설계 원칙

### 1. 실용적 Clean Architecture

- 과도한 추상화 제거
- 필요한 곳에만 인터페이스 사용
- 레이어 분리는 명확하게 유지

### 2. DDD 적용

- Bounded Context 기반 도메인 분리
- Aggregate Root 중심 설계
- 화면 구성에 맞춘 도메인 구조

### 3. Kotlin 스타일

- Data Class 활용
- Nullable 타입 명시적 처리
- Extension Function 활용 (예정)

---

## 📝 라이선스

MIT License

---

## 👨‍💻 개발자

- **GitHub**: [EATi Backend Repository](https://github.com/your-username/EATi_Back)
